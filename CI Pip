on:
  push:
    branches:
      - 47546  # or your branch name

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code (clone repo)
        uses: actions/checkout@v3

      - name: Simulate Build Step
        run: |
          echo "Building project..."
          ls -la
